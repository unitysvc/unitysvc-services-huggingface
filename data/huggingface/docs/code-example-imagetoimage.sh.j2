curl -X POST \
  -H "Authorization: Bearer $UNITYSVC_API_KEY" \
  -F "image=@input.png" \
  -F 'prompt=Turn this into a cinematic cyberpunk scene' \
  -F 'strength=0.6' \
  ${SERVICE_BASE_URL}/models/{{offering.name}} \
  --output output.png
