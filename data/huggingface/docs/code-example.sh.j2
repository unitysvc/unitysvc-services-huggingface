curl ${SERVICE_BASE_URL}/chat/completions \
  -H "Authorization: Bearer $UNITYSVC_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "${MODEL:-{{offering.name}}}",
    "messages": [
      { "role": "user", "content": "How many G'\''s are in huggingface?" }
    ]
  }'
